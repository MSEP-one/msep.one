// holds all the shader constants which are relevant to the cpu
// Those can be accessed on cpu through RenderingUtils class
// They are also shared through several shaders

const float BOND_HIGHLIGHT_FACTOR = 2.0;
const float DIMM_FACTOR = 0.3;
